
using System;
using System.IO;

namespace Nsm
{
	
	
	public sealed class Globals
	{
		
		public const string SPECS_DIR = "@expanded_datadir@/new-stuff-manager/specs";
	    public const string GPG_BIN = "@GPG@";
	    public readonly string LOCAL_CACHE_DIR;
	    
	    public static readonly Globals Instance = new Globals();
	    
	    private Globals()
	    {
            this.LOCAL_CACHE_DIR = Path.Combine( Environment.GetFolderPath(Environment.SpecialFolder.Personal), Path.Combine(".nsm","cache") );	         
	    }
	    
	}
}
